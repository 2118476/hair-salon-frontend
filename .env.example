# Hair Salon Frontend Environment Variables
# Copy this file to .env and adjust as needed. Vite only exposes variables
# prefixed with VITE_ to client code.

# Backend API base URL (no trailing slash, no /api/v1 suffix — the client adds it)
VITE_API_URL=http://localhost:8080
